node_modules/
.next/
dist/
.turbo/
.vercel/
.env*
!.env.example
*.log
.DS_Store
playwright-report/
test-results/
coverage/
apps/web/public/media/reel-stills/*.png
!apps/web/public/media/reel-stills/manifest.json

# Strategy artefacts: the canonical source is the .md. Binaries (board decks,
# exports) live alongside it for sharing but are not committed.
docs/strategy/*.docx
docs/strategy/*.pptx
docs/strategy/*.pdf

# Vercel CLI token cache — never commit (holds a live access token).
.vercelrc.json
apps/web/.vercelrc.json

# TypeScript incremental build cache
*.tsbuildinfo
.vercel
