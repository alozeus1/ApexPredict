node_modules/
.next/
dist/
.turbo/
.vercel/
.vercelrc.json
.env*
!.env.example
*.log
.DS_Store
playwright-report/
test-results/
coverage/
apps/web/public/media/reel-stills/*.png
!apps/web/public/media/reel-stills/manifest.json

# strategy artefacts — binaries live alongside the canonical .md for sharing,
# but only the .md is committed to keep the repo lean
docs/strategy/*.docx
docs/strategy/*.pptx
docs/strategy/*.pdf

# TypeScript incremental build cache
*.tsbuildinfo
.vercel
